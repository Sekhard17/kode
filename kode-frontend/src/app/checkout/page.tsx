import { CheckoutView } from '@/features/checkout';

export const metadata = {
    title: 'Checkout | KODE',
    description: 'Finaliza tu compra en KODE',
};

export default function CheckoutPage() {
    return <CheckoutView />;
}
